apiVersion: v1
kind: Service
metadata:
  name: {{ include "compage.fullname" . }}
  labels:
    {{- include "compage.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "compage.selectorLabels" . | nindent 4 }}
