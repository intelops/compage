apiVersion: v1
kind: Secret
metadata:
  name: db-cassandra-credentials
  labels:
    {{- include "compage.app.labels" . | nindent 4 }}
type: Opaque
stringData:
  CASSANDRA_CONTACT_POINTS: {{ .Values.cassandra.contactPoints }}
  CASANDRA_LOCAL_DATA_CENTER: {{ .Values.cassandra.localDataCenter }}
  CASANDRA_KEYSPACE: {{ .Values.cassandra.keyspace }}
  CASSANDRA_USERNAME: {{ .Values.cassandra.username }}
  CASSANDRA_PASSWORD: {{ .Values.cassandra.password }}