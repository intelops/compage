apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "compage.fullname" . }}-test-connection"
  labels:
    {{- include "compage.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "compage.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
