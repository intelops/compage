import React from 'react';

interface ContextWorkspaceProps {
}

export const ContextWorkspace = (props: ContextWorkspaceProps) => {
    return <React.Fragment>
        <div className="contextMenu">
            This is the Workspace.
        </div>
    </React.Fragment>;
}