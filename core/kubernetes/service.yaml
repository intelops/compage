apiVersion: v1
kind: Service
metadata:
  namespace: compage
  labels:
    app: core
  name: core
spec:
  ports:
  - port: 50051
    targetPort: grpc
    protocol: TCP
    nodePort: 30101
  selector:
    app: core
  type: NodePort
